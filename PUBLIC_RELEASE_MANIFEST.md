# 公开发布清单

本文件记录公开源码快照的边界，方便维护者在后续发布前复核。

## 应当包含

- `apps/web` 功能源码；
- 通用脚本、数据库结构和迁移；
- 使用原创虚构内容的测试；
- 许可证、归属、贡献和安全文档；
- 可公开的品牌展示资源与第三方许可证。

## 必须排除

- 真实书籍、真实原文库与译文；
- 预生成问答、来源指纹和生产搜索索引；
- PostgreSQL、D1、SQLite、对象存储或缓存导出；
- 用户、投稿、标注、登录、邮件与支付数据；
- `.env*`、`.dev.vars`、`.local-secrets`、`.vercel` 和真实密钥；
- 收款二维码及其他支付凭据；
- 宗教经典、宗教解释、宗教角色及宗教活动内容；
- 募捐、赞助、收款、支付页面、接口、环境变量和数据库结构；
- 原私有仓库的 Git 历史。

## 每次发布前

1. 对完整仓库运行密钥扫描和敏感内容关键词扫描。
2. 确认演示数据均为原创、虚构且非宗教的内容。
3. 运行 `pnpm run typecheck`、`pnpm test` 和 `pnpm run build:next`。
4. 检查 390px、768px 和桌面布局。
5. 核对提交身份和 `Required Notice:`。
6. 在公开仓库前再次确认 GitHub 仓库可见性。
